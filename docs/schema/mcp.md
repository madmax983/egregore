# eg mcp Tool I/O Contract — v1

**Status:** Frozen. This document is the source of truth for the `eg mcp`
tool payloads; `src/mcp.rs` and `src/mcp_contract.rs` must conform to it.
Issue #194.

**Machine-readable twin:** `src/mcp_contract.rs` publishes a Draft 2020-12
JSON Schema for every tool's success response (`response_schema`) and for
the shared error envelope (`error_schema`). The schemas are also published
as checked-in files — `docs/schema/mcp/<tool>.schema.json` and
`docs/schema/mcp/error.schema.json`, generated by
`cargo run --example gen_mcp_schemas` — so agent clients that cannot call
into Rust still get the machine-readable contract. The conformance test
`tests/integration/mcp_contract.rs` asserts that the real output of every
shipped tool validates against its schema, and that the checked-in files
never drift from the registered schemas. If this document and the schemas
ever disagree, fix the schemas — the doc owns *what* is exposed.

---

## 1 — Contract version

Every `eg mcp` server advertises **`mcp_contract_version`** — currently
**`1`** — in its MCP `initialize` response (`instructions` field), so a
connected agent can read it without inspecting source, without calling a
tool, and without a running daemon or store:

```
mcp_contract_version=1 (frozen tool I/O contract, issue #194; see docs/schema/mcp.md)
```

`mcp_contract_version` is deliberately distinct from two other versions:

| Version | What it versions | Where it lives |
|---|---|---|
| `mcp_contract_version` | The tool *payload* shapes below | Server `initialize` instructions; `mcp_contract::MCP_CONTRACT_VERSION` |
| rmcp protocol version | The MCP *transport* envelope (`initialize`/`protocolVersion`, JSON-RPC framing) | Negotiated by the rmcp crate; says nothing about tool result semantics |
| record `schema_version` | The *stored* `GraphRecord` shapes | Per-record `(domain, kind, version)`; governed by `docs/schema/schema-versioning.md` |

An agent binds to field names *inside* tool results; only
`mcp_contract_version` tells it when those names may have changed.

---

## 2 — Versioning policy: breaking vs additive

**Breaking-change class** — any of these bumps `mcp_contract_version`:

- field removal from a tool response or the error envelope
- field rename (including renaming a map key such as a `domain_counts`
  category label, or an error `code` identifier change)
- field type change (e.g. `string` → `integer`, `object` → `string`,
  required-ness flip in either direction)
- semantic change of an existing field (the name stays but what it *means*
  changes — e.g. `records` starting to exclude tombstones)
- error `code` identifier rename

**Additive-change class** — never bumps `mcp_contract_version`:

- a new tool (registers its schema first — see §7)
- a new optional input parameter on an existing tool
- a new field in a tool response or the error envelope
- a new error `code` (the envelope shape is unchanged)

The published schemas declare `additionalProperties: true` at every object
level, so additive fields validate against the frozen schema: an
additive-only addition keeps the conformance test green *and* the version
unchanged. That is the success metric of #194, and it is asserted in CI by
the conformance test.

---

## 3 — Field stability tiers

Per-field tiers mirror the precedent of ADR
`0005-codex-field-stability-tiers.md`, reduced to the two tiers that matter
for an agent binding to payloads:

- **stable** — the field's name, type, and semantics are frozen. An agent may
  bind to it unconditionally. Changing it is a breaking-change class and
  bumps `mcp_contract_version`.
- **provisional** — the field ships and is documented, but its exact shape
  is still settling. An agent should read it defensively (tolerate absence,
  do not key long-lived logic on its exact value). Promoting a field from
  provisional to stable is additive (no bump); *changing* a provisional
  field's shape is still a breaking-change class once agents are known to
  bind to it — when in doubt, bump.

The tier of every response field is recorded in the per-tool tables below.

---

## 4 — Tools

All tools are read-only. All tool *inputs* are JSON objects; every tool
accepts `data_dir` (default `.egregore`) and `repo_path` (default: the
server's current directory) to locate the store and the working tree the
freshness verdict is computed against.

### 4.1 `inspect_store`

Structured store-inspection summary: record counts by domain, schema
versions, and repository identities. Requires a running local daemon when
called over the wire (the `tool_inspect_store_from_records` helper used in
tests takes records directly).

**Inputs**

| Parameter | Type | Required | Notes |
|---|---|---|---|
| `data_dir` | string | no | AletheiaDB data directory (default `.egregore`) |
| `repo_path` | string | no | Working tree for the freshness verdict (default `.`) |

**Success response**

| Field | Type | Tier | Notes |
|---|---|---|---|
| `ok` | `true` | stable | |
| `snapshot_timestamp` | string \| null | stable | RFC 3339, forwarded from the daemon |
| `records` | integer | stable | Total records **including** unknown-schema rows |
| `nodes` | integer | stable | |
| `edges` | integer | stable | |
| `tombstones` | integer | stable | |
| `diagnostics` | integer | provisional | Diagnostic-node count; classification may widen |
| `domain_counts` | object | stable | Category label → `{ "<kind> v<version>": count }`. The category-label vocabulary is frozen (renaming a label is a breaking change) |
| `schema_versions` | object | stable | `"<domain>:<kind>:<version>"` → count |
| `unknown_schema_versions` | object | provisional | Same key format; keys appear only for unrecognized record versions |
| `repositories` | array | stable | Items: `{ "id": string, "identity_summary": string }` |
| `repositories[].identity_summary` | string | provisional | Free-form `"<source>: <canonical>"` rendering |
| `freshness` | object | stable | §6 |

### 4.2 `symbol_context`

Evidence-backed context for a named code symbol, trust-separated by domain.

**Inputs**

| Parameter | Type | Required | Notes |
|---|---|---|---|
| `symbol_name` | string | yes | Exact symbol name; empty → `missing_argument` error |
| `data_dir` | string | no | |
| `repo_path` | string | no | |

**Success response**

| Field | Type | Tier | Notes |
|---|---|---|---|
| `ok` | `true` | stable | |
| `symbol_name` | string | stable | Echo of the query |
| `source_facts` | array | stable | Deterministic code-graph rows (§5.1) |
| `topology_edges` | array | provisional | Code-graph topology edges (§5.4); section is newer than the rest |
| `observations` | array | stable | Agent-authored rows (§5.2) — never treat as source truth |
| `project_state` | array | stable | Tasks/ACs linked to the symbol (§5.3) |
| `artifacts` | array | stable | §5.3 |
| `verification_evidence` | array | stable | §5.3 |
| `drift_history` | array | stable | Semantic-drift rows (§5.5); the *resolved* `repo_relative_path`/`span` inside each row are provisional (resolution heuristic) |
| `unresolved` | array | provisional | Dangling references (§5.6) |
| `store_coverage` | object | provisional | Store-level domain presence flags (§5.7); added by #196 |
| `freshness` | object | stable | §6 |

Empty result: a symbol with no records returns the **`no_match`** error
(§5.8), never an empty `ok: true` payload — so "not found" is unambiguous.

### 4.3 `task_evidence`

Evidence-backed context for a task: canonical record ID, GitHub URL,
GitHub short handle, or local JSONL handle.

**Inputs**

| Parameter | Type | Required | Notes |
|---|---|---|---|
| `id_or_handle` | string | yes | Canonical ID, GitHub URL, `owner/repo#num`, or `path.jsonl:local_id` |
| `data_dir` | string | no | |
| `repo_path` | string | no | |

**Success response**

| Field | Type | Tier | Notes |
|---|---|---|---|
| `ok` | `true` | stable | |
| `task_id` | string | stable | Resolved canonical task record ID |
| `tasks` | array | stable | The anchor task row(s) (§5.3) |
| `acceptance_criteria` | array | stable | Linked-item rows (§5.3); a row with `status: "verified"` may carry a nested `verification_record` object (provisional) |
| `source_facts` | array | stable | §5.1 |
| `observations` | array | stable | §5.2 |
| `artifacts` | array | stable | §5.3 |
| `verification_evidence` | array | stable | §5.3 |
| `reviews` | array | stable | §5.3 |
| `external_links` | array | stable | §5.3 |
| `unresolved` | array | provisional | §5.6 |
| `store_coverage` | object | provisional | §5.7 |
| `freshness` | object | stable | §6 |

Empty result: unknown handle → `no_match`; handle matching several tasks →
`ambiguous_handle` (with `candidates`); unrecognized handle format →
`unsupported_handle` (with `message`). See §5.8.

### 4.4 `store_freshness`

The store-freshness verdict for the whole store — the same verdict the
per-tool `freshness` objects carry — without requiring a symbol or task
argument. Requires a running local daemon when called over the wire.

**Inputs**

| Parameter | Type | Required | Notes |
|---|---|---|---|
| `repo_path` | string | no | |
| `data_dir` | string | no | |

**Success response**

| Field | Type | Tier | Notes |
|---|---|---|---|
| `ok` | `true` | stable | |
| `freshness` | object | stable | §6 |

---

## 5 — Shared shapes

### 5.1 Source-fact row

`record_id` (string, stable), `kind` (string, stable), `trust` (string,
stable — closed vocabulary: `source_derived`, `verification_evidence`,
`agent_verified`, `agent_unverified`, `agent_contradicted`,
`project_state`, `artifact`, `runtime_observation`, `other`; shared with the
CLI and daemon), `name` (string|null, stable), `repo_relative_path`
(string|null, stable), `span` (object|null, stable — `start_byte`,
`end_byte`, `start_line`, `end_line`, optional `start_column`/`end_column`),
`git_commit` (string|null, stable), `valid_time` (string|null, stable),
`language` (string|null, stable), `symbol_kind` (string|null, stable).

### 5.2 Observation row

`record_id`, `kind`, `trust` as above (all stable). `summary`, `text`,
`provenance_handle` (`"<agent_id>:<session_id>"`), `agent_id`,
`session_id`, `observed_at`, `confidence`, `failure_kind`
(all string|null, stable), `exit_code` (integer|null, stable),
`evidence_links` (array of `{ target_record_id, target_domain, relation,
confidence }` (all string|null), stable). Never inlines raw transcript
text beyond the redacted `text`/`summary` the CLI emits.

### 5.3 Linked-item row

The generic row for tasks, acceptance criteria, artifacts, verification
evidence, reviews, and external links. `record_id`, `kind`, `trust`
(stable). All other fields are nullable and stable unless noted:
`summary`, `title`, `name`, `text`, `status`, `verification_kind`,
`executed_at`, `evidence_quality`, `source_artifact_path`,
`source_artifact_hash`, `repo_relative_path`, `edit_kind`,
`patch_status`, `patch_bytes_hash`, `base_commit`, `producer_session_id`,
`author`, `url`, `system_native_id`, `validation_summary` (provisional —
currently renders the `<summarized>` redaction marker), `exit_code`
(integer|null), `patch_bytes_size` (integer|null), `target_files`
(array|null of strings), `stdout_handle`/`stderr_handle`/`body_handle`
(object|null — citation metadata `{ hash, bytes }` only, never inlined
bytes), `patch_handle` (object|null — `{ path }` only), `evidence_links`
(array, §5.2).

### 5.4 Topology-edge row

`record_id`, `trust`, `label`, `source_id`, `target_id` (strings, stable as
names; the section itself is provisional), `summary`, `git_commit`,
`valid_time` (string|null).

### 5.5 Drift-history row

`record_id`, `trust` (stable), `score` (number, stable),
`before_commit`/`after_commit`/`before_valid_time`/`after_valid_time`/
`embedding_model` (strings, stable), `repo_relative_path` (string,
provisional), `span` (object, provisional) — the same citable handle the
CLI and daemon render.

### 5.6 Unresolved-reference row (provisional)

`source_record_id`, `target_handle`, `relation`, `target_domain` (strings),
`verification_status` (always `"unresolved"`).

### 5.7 `store_coverage` (provisional)

`{ code_graph, agent_memory, project, artifact, verification }` — booleans.
Semantics (issue #196): `false` + empty section = the domain is absent from
the store; `true` + empty section = the queried entity has no records in
that domain.

### 5.8 Error and empty-result shapes

Every error shares one stable envelope:

```json
{ "ok": false, "error": { "code": "<stable-code>", "...": "code-specific detail" } }
```

| Code | Meaning | Detail fields | When |
|---|---|---|---|
| `no_match` | Symbol/task not found | `symbol_name` or `id_or_handle` | The store is readable but the entity is absent — **not** an error in the store itself |
| `ambiguous_handle` | Handle matched >1 task | `handle`, `candidates` (array of IDs) | Caller must disambiguate |
| `unsupported_handle` | Handle format unrecognized | `handle`, `message` | Caller bug |
| `missing_argument` | Required argument empty | `field`, `message` | Caller bug |
| `daemon_not_running` | No live daemon for the data dir | `message` | **Store missing or unreadable** — retry after `eg daemon` |
| `daemon_stale` | Daemon metadata stale | `message` | **Store missing or unreadable** — restart the daemon |

The agent rule: `no_match` ⇒ the entity does not exist; `daemon_not_running`
/ `daemon_stale` ⇒ the store cannot be read. The two are never conflated.
Error `code` identifiers are stable; adding a new code is additive.

---

## 6 — The `freshness` object

Stamped onto every successful tool response (issue #220). It is a trust
signal, never suppression: the stamp is purely additive and the answer
payload is preserved. Error payloads (`ok: false`) never carry it.

Reuses the store-freshness contract from #186 — no new vocabulary:

| Field | Type | Tier | Notes |
|---|---|---|---|
| `verdict` | string | stable | `fresh` \| `stale_head` \| `stale_dirty` \| `unknown` — the same code `eg freshness` reports |
| `fresh` | boolean | stable | `true` only when `verdict == "fresh"` |
| `repository_id` | string | stable | Which repository the verdict was computed for |
| `repo_path` | string | stable | Which working tree it was compared against |
| `stored_snapshot` | object | stable | `{"head": {"state": "commit"\|"no_git"\|"unborn_head", "sha"?}, "dirty": bool, "repository_id": string, "scanned_at": string}` — or the explicit `{"state": "pre_stamping"}` marker for a store that predates snapshot stamping (never a silent absence, never a false `fresh`) |
| `current_head` | object | stable | `{"state": "commit"\|"no_git"\|"unborn_head", "sha"?}` — the live working-tree probe (read-only, `GIT_OPTIONAL_LOCKS=0`) |
| `current_dirty` | boolean | stable | |
| `message` | string | stable | Human-readable one-line explanation |

---

## 7 — Release gate for new tools

Adding an MCP tool (queued: #181, #182, #183, #188) is a **release-gated**
change. Before a new tool ships, its author must, in the same commit:

1. Enumerate the tool in `docs/schema/mcp.md` §4: input parameters and the
   success-response shape with per-field stability tiers (§3).
2. Publish its success-response JSON Schema in `src/mcp_contract.rs`
   (`response_schema`), following the breaking/additive rules of §2, and
   regenerate the checked-in file
   (`cargo run --example gen_mcp_schemas`).
3. Extend `MCP_CONTRACT_TOOLS` and the conformance test so the new tool's
   real output validates against its schema.
4. Confirm `mcp_contract_version` is **unchanged** (new tools are additive
   by definition). If shipping the tool required changing an existing
   tool's payload in a breaking-change class, bump the version, document
   the break, and say so in the commit message.

A tool that lands without its schema registered is a contract violation —
reviewers should block it the way they would block an undocumented daemon
route.
